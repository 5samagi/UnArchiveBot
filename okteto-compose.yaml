version: "20.04"
services:
  worker:
    build: .
     environment:
      - APP_ID=${APP_ID}
      - API_HASH=${API_HASH}
      - BOT_TOKEN=${BOT_TOKEN}
      - AUTH_IDS=${AUTH_IDS}
      - BOT_USERNAME=${BOT_USERNAME}
      - OWNER_ID=${OWNER_ID}
